---
title: Sitemap
layout: page
permalink: /sitemap/
---

Looking to explore the site? Here are some pages you might want to check out.

<ul class="h-feed">
  {%- assign items = collections.all | sort: "url" %}
  {%- for item in items %}
    {%- assign segmentCount = item.url | split: '/' | size | minus: 1 %}
    {%- if segmentCount <= 1 and not item.data.excludeFromSitemap %}
    {%- assign nextPageStartsWithSomethingElse = items[forloop.index].url | split:  item.url | first %}
    <li class="h-entry">
      <a href="{{ item.url }}">{{ item.url | remove_last: '/' | default: '/' }}</a>
    {%- if nextPageStartsWithSomethingElse == blank %} <span title="This page contains more sub-pages">📁</span>{% endif %}
    </li>
    {%- endif %}
  {%- endfor %}
<ul>
